export  enum TypeOfCall {
    Initial = "I",
    FollowUp = "F"
}

export enum RecordMode {
    Create = "Create",
    Edit = "Edit"
}

export enum FormAction {
    CancelUpdate = -1,
    Update = 1
}

export enum AssigmentMode {
    NewTask = "NewTask",
    EditTask = "EditTask",
    ReassignTask = "ReassignTask"
}

export enum FileDownloadType {
    AssignedDate = 1,
    InitDate = 2,
    CompletionDate = 3
}
