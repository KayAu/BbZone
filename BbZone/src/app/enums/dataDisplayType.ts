export enum DataDisplayType {
    text,
    badge,
    tick
}

export enum ControlType {
    label,
    textbox,
    textarea,
    select,
    checkbox,
    editabledropdown,
    cascadeDropdown,
    dateRange,
    password,
    number,
    customerFinder
}

export enum CustomerSearchType {
    completed,
    commissionClaimed
}